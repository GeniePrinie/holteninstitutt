import React from "react";
import { Container, Typography } from "@mui/material";

const Contact: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Kontakt
      </Typography>
      <Typography>This is the Kontakt page.</Typography>
    </Container>
  );
};

export default Contact;
